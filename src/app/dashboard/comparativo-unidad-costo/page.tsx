import { ComparativoDashboard } from "./comparativo-dashboard";

export const metadata = {
  title: "Comparativo por Unidad de Costo | Costos Lomas",
};

export default function ComparativoPage() {
  return <ComparativoDashboard />;
}
